// create observer
const observer = new IntersectionObserver(entries => {
//     // loop over the entries
//     entries.forEach(entry => {
//         if (entry.isIntersection) {
//             // its visible. add animation class
//             entry.target.classList.add('larger');
//         }
//     });
// });

// tell th eobserver which elements to track
// observer.observe(document.querySelector('.sub-headings'));

